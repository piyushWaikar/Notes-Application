<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #1e1e1e;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            color: #d3d3d3;
        }
        .container {
            background: #2e2e2e;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
            width: 320px;
        }
        h1 {
            text-align: center;
            color: #007bff;
            margin-bottom: 20px;
            font-size: 24px;
        }
        label {
            display: block;
            margin-bottom: 10px;
            color: #d3d3d3;
            font-weight: bold;
            margin-right: 20px;
        }
        input[type="text"], textarea {
            width: 100%;
            padding: 10px;
            margin-top: 5px;
            margin-bottom: 15px;
            border: 1px solid #555;
            border-radius: 5px;
            background-color: #3c3c3c;
            color: #d3d3d3;
            font-size: 16px;
        }
        input[readonly] {
            background-color: #2e2e2e;
            color: #777;
            cursor: not-allowed;
        }
        textarea {
            height: 100px; /* Make the content box larger */
            resize: none; /* Disable resizing */
        }
        button {
            width: 100%;
            padding: 12px;
            background-color: #007bff;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
            transition: background-color 0.3s ease;
        }
        button:hover {
            background-color: #0056b3;
        }
        hr {
            border: 0;
            border-top: 1px solid #555;
            margin: 20px 0;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Edit Note</h1>
        <form action="/posts/edit/<%= id %>?_method=PATCH" method="post">
            <label for="name">Topic
                <input type="text" name="username" value="<%= username %>" readonly>
            </label>
            <hr>
            <label for="content">Description
                <textarea placeholder="Description" name="content"><%= content %></textarea>
            </label>
            <hr>
            <button type="submit">Submit</button>
        </form>
    </div>
</body>
</html>
